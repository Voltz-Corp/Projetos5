{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API server",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "start": "node index.js",
    "migrate": "node migrate.js",
    "db:sync": "node -e \"import('./database.js').then(({sequelize}) => sequelize.sync({force: true}).then(() => console.log('Database synced')).catch(console.error))\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "sequelize": "^6.37.7"
  }
}